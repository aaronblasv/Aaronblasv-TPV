meta {
  name: Create Family
  type: http
  seq: 2
}

post {
  url: {{base_url}}/families
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "name": "Bebidas",
      "tax_id": "UUID_DE_UN_TAX"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
