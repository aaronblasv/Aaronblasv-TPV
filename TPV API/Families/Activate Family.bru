meta {
  name: Activate Family
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/families/{uuid}/activate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
