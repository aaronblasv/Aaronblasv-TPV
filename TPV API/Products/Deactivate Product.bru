meta {
  name: Deactivate Product
  type: http
  seq: 6
}

patch {
  url: {{base_url}}/products/{uuid}/deactivate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
